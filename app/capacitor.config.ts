import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tomyail.pushover.scheduler',
  appName: 'Pushover Scheduler',
  webDir: '../web/dist'
};

export default config;
