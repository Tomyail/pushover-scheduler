{
    "name": "app",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "cap:sync": "cd ../web && npm run build && cd ../app && npx cap sync",
        "ios": "npx cap open ios",
        "android": "npx cap open android"
    },
    "dependencies": {
        "@capacitor/android": "^8.0.0",
        "@capacitor/core": "^8.0.0",
        "@capacitor/ios": "^8.0.0"
    },
    "devDependencies": {
        "@capacitor/cli": "^8.0.0"
    }
}